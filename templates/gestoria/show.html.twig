{% extends 'base.html.twig' %}

{% block title %}Gestoria{% endblock %}

{% block body %}
    <h1>Gestoria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gestorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ gestorium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gestoria_index') }}">back to list</a>

    <a href="{{ path('gestoria_edit', {'id': gestorium.id}) }}">edit</a>

    {{ include('gestoria/_delete_form.html.twig') }}
{% endblock %}