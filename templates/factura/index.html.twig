{% extends 'base.html.twig' %}

{% block title %}Factura index{% endblock %}
{% block body %}

<div id="facturanew"> 
    <h1>Factura</h1>
    <a class="btn btn-outline-info" href="{{ path('factura_new') }}" role="button">Nueva factura</a>
</div>

<table class="table">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Nº</th>
      <th scope="col">Nombre</th>
      <th scope="col">Fecha</th>
      <th scope="col">Factura completa</th>
    </tr>
  </thead>
  <tbody>
        {% for factura in facturas %}
            <tr>
                <td>{{ factura.id }}</td>
                <td>
                    <a href="{{ path('factura_show', {'id': factura.id}) }}">show</a>
                    <a href="{{ path('factura_edit', {'id': factura.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
</table>
  

   

   
{% endblock %}