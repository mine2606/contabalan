{% extends 'base.html.twig' %}

{% block title %}Operacion index{% endblock %}

{% block body %}
    <h1>Operacion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for operacion in operacions %}
            <tr>
                <td>{{ operacion.id }}</td>
                <td>
                    <a href="{{ path('operacion_show', {'id': operacion.id}) }}">show</a>
                    <a href="{{ path('operacion_edit', {'id': operacion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('operacion_new') }}">Create new</a>
{% endblock %}