{% extends 'base.html.twig' %}

{% block title %}Administrador{% endblock %}

{% block body %}
    <h1>Administrador</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ administrador.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('administrador_index') }}">back to list</a>

    <a href="{{ path('administrador_edit', {'id': administrador.id}) }}">edit</a>

    {{ include('administrador/_delete_form.html.twig') }}
{% endblock %}